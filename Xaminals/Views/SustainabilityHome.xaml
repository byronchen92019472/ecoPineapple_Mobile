<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             x:Class="Xaminals.Views.SustainabilityHome"
             xmlns:data="clr-namespace:Xaminals.Data"
             Title="Sustainability">
    <ContentPage.Resources>
            <Color x:Key="Primary">#96d1ff</Color>
            <Color x:Key="LightTextColor">#999999</Color>
    </ContentPage.Resources>

    <ScrollView>
        <CollectionView Margin="20"
                        ItemsSource="{x:Static data:SustainabilityData.Sustainability}"
                        SelectionMode="Single"/>
        <!--<StackLayout>
            <Label Text="Sustainability Issues" Style="{StaticResource headingStyle}"/>
            <Label x:Name="SustainabilityTextBody" Style="{StaticResource textbodyStyle}"/>
            <Image Source="sustain.png" Style="{StaticResource largeimageStyle}"/>
            <BoxView BackgroundColor="Red" HeightRequest="600" WidthRequest="150" />
            <BoxView BackgroundColor="Blue" HeightRequest="600" WidthRequest="150" />
            <BoxView BackgroundColor="Green" HeightRequest="600" WidthRequest="150" />
            <BoxView BackgroundColor="GreenYellow" HeightRequest="600" WidthRequest="150" />
        </StackLayout>-->
    </ScrollView>
</ContentPage>